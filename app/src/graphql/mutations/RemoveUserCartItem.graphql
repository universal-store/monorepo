mutation RemoveUserCartItem($itemBarcodeId: String!, $userId: uuid!) {
    delete_UserCartItem(where: { itemBarcodeId: { _eq: $itemBarcodeId }, userId: { _eq: $userId }}) {
        affected_rows
    }
}
