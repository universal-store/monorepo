mutation SignUp($email: String!, $firstName: name!, $lastName: name, $password: String!) {
  insert_User_one(object: {email: $email, firstName: $firstName, lastName: $lastName, password: $password}) {
    ...UserInfo
  }
}
