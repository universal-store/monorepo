{
  "name": "@universal-store/server",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "start": "node dist/server",
    "clean": "rm -rf dist",
    "build": "yarn clean && tsc",
    "generate": "GENERATE_ARTIFACTS=true ts-node --transpile-only src/schema",
    "dev": "source ../database/.env && GENERATE_ARTIFACTS=true ts-node-dev --no-notify --respawn --transpile-only src/server",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "@nexus/schema": "^0.15.0",
    "@types/node-fetch": "2.5.7",
    "apollo-server": "2.14.3",
    "graphql": "14.5.8",
    "nexus-plugin-prisma": "0.17.0"
  },
  "devDependencies": {
    "@types/node": "14.0.13",
    "@typescript-eslint/eslint-plugin": "^3.3.0",
    "@typescript-eslint/parser": "^3.3.0",
    "eslint": "^7.4.0",
    "eslint-config-prettier": "^6.11.0",
    "ts-node": "8.10.2",
    "ts-node-dev": "1.0.0-pre.56",
    "typescript": "3.9.7"
  }
}
