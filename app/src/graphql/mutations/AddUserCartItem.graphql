mutation AddUserCartItem($itemBarcodeId: String!, $userId: String!) {
    insert_UserCartItem_one(object: { itemBarcodeId: $itemBarcodeId, userId: $userId }) {
        StoreItem {
            ...StoreItemInfo
        }
    }
}
