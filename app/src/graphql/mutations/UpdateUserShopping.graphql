mutation UpdateUserShopping($currentTime: timestamptz!, $userId: String!, $lastItem: String!) {
    update_UserShopping(_set: { updated_at: $currentTime, lastItem: $lastItem }, where: { userId: { _eq: $userId } }) {
        affected_rows
    }
}
