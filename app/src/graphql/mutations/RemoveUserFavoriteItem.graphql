mutation RemoveUserFavoriteItem($itemBarcodeId: String!, $userId: String!) {
    delete_UserFavoriteItem(where: { itemBarcodeId: { _eq: $itemBarcodeId }, userId: {_eq: $userId } }) {
        affected_rows
    }
}
