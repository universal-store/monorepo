# @format

fragment MarkerInfo on Store {
  id
  name
  address
  category
  location
}

fragment StoreInfo on Store {
  id
  name
  address
  category
  location
  description
  StorePic {
    size64
    size128
  }
}
