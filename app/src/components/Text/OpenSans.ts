/** @format */

import styled from 'styled-components/native';

export const OpenSansSemiBoldSmall = styled.Text`
  font-size: 18px;
  line-height: 25px;
  font-family: OpenSans-SemiBold;
  color: ${({ theme }) => theme.colors.gray[1]};
`;
