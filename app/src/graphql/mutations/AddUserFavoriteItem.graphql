mutation AddUserFavoriteItem($itemBarcodeId: String!, $userId: uuid) {
    insert_UserFavoriteItem_one(object: {itemBarcodeId: $itemBarcodeId, userId: $userId}) {
        StoreItem {
            ...StoreItemInfo
        }
    }
}
